import { UnidadeLitragemPipe } from './unidade-litragem.pipe';

describe('UnidadeLitragemPipe', () => {
  it('create an instance', () => {
    const pipe = new UnidadeLitragemPipe();
    expect(pipe).toBeTruthy();
  });
});
